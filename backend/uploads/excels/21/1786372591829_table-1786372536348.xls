用例名称,操作步骤,预期结果
TC01-完整表单正常提交,1. 在「Customer name」输入框输入「张三」<br>2. 在「Telephone」输入框输入「13800138000」<br>3. 在「E-mail address」输入框输入「<zhangsan@test.com>」<br>4. 单选「Pizza Size」为「Large」<br>5. 勾选「Pizza Topping」的「Bacon」和「Cheese」<br>6. 在「Preferred delivery time」输入「18:30」<br>7. 在「Delivery instructions」输入「请放在门口」<br>8. 点击「Submit order」按钮,1. 页面跳转至 `/post`<br>2. 响应 JSON 中 `form.custname` 值为「张三」<br>3. `form.size` 值为「large」<br>4. `form.topping` 包含「bacon」和「cheese」<br>5. `form.delivery` 值为「18:30」
TC02-仅填写必填项提交,1. 在「Customer name」输入「李四」<br>2. 在「Telephone」输入「13900139000」<br>3. 单选「Pizza Size」为「Medium」<br>4. 点击「Submit order」按钮,1. 页面成功跳转至 `/post`<br>2. 响应中 `form.custname` 为「李四」<br>3. `form.custemail`、`form.topping`、`form.comments` 为空或不存在<br>4. 无 400/500 错误
TC03-邮箱格式验证-无效邮箱,1. 在「Customer name」输入「王五」<br>2. 在「Telephone」输入「13700137000」<br>3. 在「E-mail address」输入「invalid-email」<br>4. 点击「Submit order」按钮,1. 浏览器阻止表单提交（HTML5 邮箱校验）<br>2. 页面停留在表单页<br>3. 「E-mail address」输入框出现浏览器默认验证提示（如「请输入包含 @ 的邮箱地址」）
TC04-邮箱格式验证-缺少域名,1. 在「E-mail address」输入「test@」<br>2. 其他必填项正常填写<br>3. 点击「Submit order」按钮,1. 浏览器阻止提交<br>2. 提示邮箱格式不完整
TC05-电话格式验证-非数字字符,1. 在「Telephone」输入「abc-def-ghij」<br>2. 其他必填项正常填写<br>3. 点击「Submit order」按钮,"1. 浏览器阻止提交（HTML5 `type=""tel""` 配合 pattern 时）或表单提交但后端原样返回<br>2. 若通过校验，响应中 `form.custtel` 值为「abc-def-ghij」"
TC06-单选按钮互斥性验证,1. 依次点击「Small」「Medium」「Large」<br>2. 观察单选按钮状态,1. 最终只有「Large」被选中<br>2. 「Small」和「Medium」自动取消选中<br>3. 提交后 `form.size` 仅包含一个值
TC07-多选框全选,1. 正常填写姓名和电话<br>2. 勾选所有 Topping：Bacon、Cheese、Onion、Mushroom<br>3. 点击「Submit order」按钮,1. 响应中 `form.topping` 包含全部四个选项值<br>2. 页面跳转成功
TC08-多选框全不选,1. 正常填写姓名、电话、邮箱<br>2. 确保所有 Topping 复选框均未勾选<br>3. 点击「Submit order」按钮,1. 页面成功跳转<br>2. 响应中 `form.topping` 为空或不存在
TC09-时间选择器边界-午夜,1. 正常填写必填项<br>2. 在「Preferred delivery time」输入「00:00」<br>3. 点击提交,1. 响应中 `form.delivery` 值为「00:00」<br>2. 无格式错误
TC10-时间选择器边界-深夜,1. 正常填写必填项<br>2. 在「Preferred delivery time」输入「23:59」<br>3. 点击提交,1. 响应中 `form.delivery` 值为「23:59」<br>2. 提交成功
TC11-文本域长文本输入,1. 正常填写必填项<br>2. 在「Delivery instructions」输入 500 字以上的中文长文本（含换行）<br>3. 点击提交,1. 响应中 `form.comments` 完整保留所有文本内容<br>2. 换行符被正确编码为 `\n` 或保留<br>3. 无截断或乱码
TC12-文本域特殊字符-XSS注入,1. 正常填写必填项<br>2. 在「Delivery instructions」输入 `<script>alert('xss')</script>`<br>3. 点击提交,1. 页面跳转至 `/post`<br>2. 响应中 `form.comments` 原样返回脚本标签文本（httpbin 不做过滤，用于观察原始提交）<br>3. 浏览器不执行 alert（响应为 JSON，非 HTML 渲染）
TC13-空表单直接提交,1. 不填写任何字段<br>2. 直接点击「Submit order」按钮,1. 页面跳转至 `/post`<br>2. 响应中 `form` 对象所有字段为空字符串或不存在<br>3. 无 500 错误（httpbin 接受空表单）
TC14-姓名字段边界-超长名称,1. 在「Customer name」输入 200 个汉字<br>2. 其他必填项正常填写<br>3. 点击提交,1. 页面成功跳转<br>2. 响应中 `form.custname` 完整显示 200 个汉字<br>3. 无截断
TC15-姓名字段边界-单字符,1. 在「Customer name」输入「A」<br>2. 其他必填项正常填写<br>3. 点击提交,1. 响应中 `form.custname` 值为「A」<br>2. 提交成功
TC16-时间选择器-无效时间格式,1. 正常填写必填项<br>2. 尝试在「Preferred delivery time」手动输入「25:00」或「12:61」（如浏览器允许）<br>3. 点击提交,1. 若浏览器支持 HTML5 time 校验，则阻止提交并提示时间无效<br>2. 若提交成功，观察后端是否接收该值
TC17-邮箱边界-最大长度,1. 在「E-mail address」输入 64 字符用户名 + 域名（如 `a...a@example.com`）<br>2. 其他必填项正常填写<br>3. 点击提交,1. 浏览器允许输入并提交<br>2. 响应中 `form.custemail` 完整保留该长邮箱地址
TC18-配料组合-单选,1. 正常填写必填项<br>2. 仅勾选「Pizza Topping」中的「Mushroom」<br>3. 点击提交,1. 响应中 `form.topping` 仅包含「mushroom」<br>2. 其他配料未选中
TC19-页面元素存在性检查,"1. 打开页面 `http://httpbin.org/forms/post`<br>2. 依次检查以下元素是否存在：<br>   - Customer name 输入框 (`name=""custname""`)<br>   - Telephone 输入框 (`name=""custtel""`)<br>   - E-mail 输入框 (`name=""custemail""`)<br>   - Size 单选组 (`name=""size""`) ×3<br>   - Topping 复选组 (`name=""topping""`) ×4<br>   - Delivery time (`name=""delivery""`)<br>   - Comments 文本域 (`name=""comments""`)<br>   - Submit 按钮",1. 所有 7 个表单字段均存在于 DOM 中<br>2. 单选按钮共 3 个（small/medium/large）<br>3. 复选框共 4 个<br>4. Submit 按钮文本为「Submit order」
TC20-表单 Action 与 Method 验证,"1. 右键查看页面源码或检查 `<form>` 标签属性<br>2. 验证 `action=""/post""` 和 `method=""post""`",1. `form` 标签的 `action` 属性值为 `/post`<br>2. `method` 属性值为 `post`<br>3. 提交后浏览器地址变为 `/post`，确认是 POST 跳转